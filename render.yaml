services:
  - type: web
    name: freegate-proxy-1
    env: node
    region: singapore
    plan: free
    buildCommand: npm install
    startCommand: node api/index.js
    envVars:
      - key: NODE_ENV
        value: production
