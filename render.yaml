services:
  - type: web
    name: freegate-stealth-proxy
    env: node
    region: singapore
    plan: free
    buildCommand: npm install
    startCommand: node api/index.js
    envVars:
      - key: PUPPETEER_SKIP_CHROMIUM_DOWNLOAD
        value: "false"
      - key: NODE_ENV
        value: production
